import AuthTitle from './components/AuthTitle';

const Login = () => {
  return (
    <main className="bg-background h-[calc(100vh-100px)] flex flex-col items-center justify-center">
      <AuthTitle />
    </main>
  );
};

export default Login;
