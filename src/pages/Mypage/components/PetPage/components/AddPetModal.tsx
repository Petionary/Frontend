import Button from '../../../../../components/Button';

const AddPetModal = () => {
  return (
    <dialog open className="w-full h-full border shadow-md">
      <Button margin="mx-auto">X</Button>
    </dialog>
  );
};

export default AddPetModal;
